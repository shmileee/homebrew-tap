{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabledManagers": ["custom.regex"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update the Alacritty cask version and DMG checksum",
      "managerFilePatterns": ["/^Casks/alacritty\\.rb$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\n\\s*version \\\"(?<currentValue>[^\\\"]+)\\\"\\n\\s*sha256 \\\"(?<currentDigest>[a-f0-9]{64})\\\""
      ],
      "versioningTemplate": "semver"
    }
  ],
  "customDatasources": {
    "alacritty-releases": {
      "defaultRegistryUrlTemplate": "https://api.github.com/repos/{{packageName}}/releases?per_page=100",
      "transformTemplates": [
        "{\"releases\": $map($[draft = false and prerelease = false], function($release) {($tag := $release.tag_name; $asset := $release.assets[name = (\"Alacritty-\" & $tag & \".dmg\")][0]; $asset ? {\"version\": $substring($tag, 1), \"releaseTimestamp\": $release.published_at, \"changelogUrl\": $release.html_url, \"sourceUrl\": \"https://github.com/alacritty/alacritty\", \"digest\": $asset.digest ? $substringAfter($asset.digest, \"sha256:\") : undefined})})}"
      ]
    }
  },
  "packageRules": [
    {
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["alacritty/alacritty"],
      "semanticCommitType": "chore",
      "semanticCommitScope": "alacritty",
      "commitMessageTopic": "Alacritty cask",
      "labels": ["dependencies"]
    }
  ]
}
